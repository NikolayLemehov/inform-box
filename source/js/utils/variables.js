const columnVisibility = {
  id: true,
  name: true,
  year: true,
  color: true,
  pantone: true,
  isSaved: false,
};

const storeId = {
  COLUMN_VISIBILITY: `column_visibility`,
};

export {columnVisibility, storeId};
